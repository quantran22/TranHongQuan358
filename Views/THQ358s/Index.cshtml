@model IEnumerable<TranHongQuan358.Models.THQ358>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.THQName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.THQGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.THQName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.THQGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.THQId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.THQId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.THQId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
